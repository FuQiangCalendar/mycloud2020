server: 
  port: 8099
  servlet:
    encoding:
      force: true
      charset: UTF-8
      enabled: true
    context-path: /

spring:
  servlet:
    multipart:
      max-request-size: 100MB
      max-file-size: 100MB 
      enabled: true
  application:
    name: projects-onlinePay                         # 指定服务名称  SERVICE-CORE